using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Nicklol : MonoBehaviour
{
    public bool Straight;
    // Start is called before the first frame update
    void Start()
    {
        Straight = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
